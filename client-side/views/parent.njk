<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    <main>
        <h1>{{ title }}</h1>
    {% block main %} No template content specified {% endblock main %}
        {% for  code, url in emojis %}
            <img src="{{ url }}" alt="{{ code }}" title="{{ code }}">
        {% endfor %}
    </main>
</body>
</html>